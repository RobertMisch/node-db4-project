const db = require("../data/db-config")

module.exports = {
    //put the helper functions in that you'll end up using
    // find,
    // findById,
    // findSteps,
    // add,
    // addStep,
    // update,
    // remove
}